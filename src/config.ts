// Site configuration
// Toggle this flag to switch between "coming soon" and "available" mode
export const config = {
  // Set to true when the product is available for download
  isProductAvailable: false,
};
